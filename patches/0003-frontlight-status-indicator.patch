diff --git a/src/activities/home/HomeActivity.cpp b/src/activities/home/HomeActivity.cpp
index fae7162a..96c94986 100644
--- a/src/activities/home/HomeActivity.cpp
+++ b/src/activities/home/HomeActivity.cpp
@@ -4,6 +4,7 @@
 #include <Epub.h>
 #include <FsHelpers.h>
 #include <GfxRenderer.h>
+#include <HalFrontlight.h>
 #include <HalDisplay.h>
 #include <HalStorage.h>
 #include <I18n.h>
@@ -1520,6 +1521,21 @@ void HomeActivity::loop() {
 
   if (quickActionsPopup.handleInput(mappedInput, [this] { requestUpdate(); })) return;
 
+  // Tapping the lamp in the top status row toggles the frontlight, as in the reader.
+  {
+    int tapX = 0;
+    int tapY = 0;
+    if (mappedInput.wasScreenTapped(tapX, tapY) &&
+        GUI.isFrontlightIndicatorTap(renderer, UITheme::getInstance().getMetrics().topPadding, tapX, tapY)) {
+      const bool lightOn = !Frontlight.isOn();
+      Frontlight.setOn(lightOn);
+      SETTINGS.frontlightOn = lightOn ? 1 : 0;
+      activityManager.persistGlobalSettings();
+      requestUpdate();
+      return;
+    }
+  }
+
   if (usesMinimalHomeInteraction()) {
     const int pressedFrontButton = mappedInput.getPressedFrontButton();
     const int releasedFrontButton = mappedInput.getReleasedFrontButton();
diff --git a/src/activities/reader/EpubReaderActivity.cpp b/src/activities/reader/EpubReaderActivity.cpp
index d02e5182..a71dc104 100644
--- a/src/activities/reader/EpubReaderActivity.cpp
+++ b/src/activities/reader/EpubReaderActivity.cpp
@@ -8,6 +8,7 @@
 #include <FontCacheManager.h>
 #include <FsHelpers.h>
 #include <GfxRenderer.h>
+#include <HalFrontlight.h>
 #include <HalStorage.h>
 #include <I18n.h>
 #include <Logging.h>
@@ -2641,6 +2642,17 @@ void EpubReaderActivity::loop() {
 #endif
 
   const auto touch = ReaderUtils::detectTouchPageTurn(renderer, mappedInput);
+  if (touch.tapped && GUI.isFrontlightIndicatorTap(renderer, UITheme::getInstance().getMetrics().topPadding,
+                                                    touch.x, touch.y)) {
+    const bool lightOn = !Frontlight.isOn();
+    Frontlight.setOn(lightOn);
+    // Save it like the other light toggles do, so restarts (e.g. into Wi-Fi
+    // mode for a sync) restore the light the user actually sees.
+    SETTINGS.frontlightOn = lightOn ? 1 : 0;
+    activityManager.persistGlobalSettings();
+    requestUpdate();
+    return;
+  }
   if (touch.tapped &&
       ReaderUtils::isBottomStatusBarTap(renderer, touch.y, UITheme::getInstance().getStatusBarHeight())) {
     if (SETTINGS.tapToHideStatusBar) {
diff --git a/src/activities/reader/TxtReaderActivity.cpp b/src/activities/reader/TxtReaderActivity.cpp
index 9b605947..2b01c3bc 100644
--- a/src/activities/reader/TxtReaderActivity.cpp
+++ b/src/activities/reader/TxtReaderActivity.cpp
@@ -3,6 +3,7 @@
 #include <BidiUtils.h>
 #include <FontCacheManager.h>
 #include <GfxRenderer.h>
+#include <HalFrontlight.h>
 #include <HalStorage.h>
 #include <I18n.h>
 #include <Memory.h>
@@ -200,6 +201,17 @@ void TxtReaderActivity::loop() {
   if (handlePinchFontResize()) return;
 #endif
   const auto touch = ReaderUtils::detectTouchPageTurn(renderer, mappedInput);
+  if (touch.tapped && GUI.isFrontlightIndicatorTap(renderer, UITheme::getInstance().getMetrics().topPadding,
+                                                    touch.x, touch.y)) {
+    const bool lightOn = !Frontlight.isOn();
+    Frontlight.setOn(lightOn);
+    // Save it like the other light toggles do, so restarts (e.g. into Wi-Fi
+    // mode for a sync) restore the light the user actually sees.
+    SETTINGS.frontlightOn = lightOn ? 1 : 0;
+    activityManager.persistGlobalSettings();
+    requestUpdate();
+    return;
+  }
   if (touch.tapped &&
       ReaderUtils::isBottomStatusBarTap(renderer, touch.y, UITheme::getInstance().getStatusBarHeight())) {
     if (SETTINGS.tapToHideStatusBar) {
diff --git a/src/components/themes/BaseTheme.cpp b/src/components/themes/BaseTheme.cpp
index 514f41f6..d4c36f3b 100644
--- a/src/components/themes/BaseTheme.cpp
+++ b/src/components/themes/BaseTheme.cpp
@@ -3,6 +3,7 @@
 #include <FreeInkUIGfxRenderer.h>
 #include <GfxRenderer.h>
 #include <HalClock.h>
+#include <HalFrontlight.h>
 #include <HalGPIO.h>
 #include <HalPowerManager.h>
 #include <HalStorage.h>
@@ -33,6 +34,23 @@ constexpr int homeMarginTop = 30;
 constexpr int roundedRaffHeaderClockYOffset = 5;
 constexpr int detachedHeaderBatteryTopInset = 5;
 
+// Status-bar-sized (20x20), sideways version of icon_lightbulb_28 /
+// icon_lightbulb_off_28 (frontlightHeaderIcons.h): downsampled to 20px and
+// rotated so the bulb head points right and the screw base points left, then
+// flipped top-to-bottom to correct for how it actually looked on real
+// hardware (the previous, unflipped revision rendered upside down).
+constexpr int frontlightStatusIconSize = 20;
+constexpr uint8_t icon_lightbulb_h_bits[] = {
+    0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xF0, 0xFF,
+    0xFF, 0xC0, 0x7F, 0xFF, 0x1E, 0x3F, 0xF3, 0x3F, 0x3F, 0xB3, 0xFF, 0x9F, 0xB3, 0xFF, 0xDF,
+    0xB3, 0xFF, 0xDF, 0xB3, 0xFF, 0x9F, 0xF3, 0x3F, 0x3F, 0xFF, 0x1E, 0x3F, 0xFF, 0xC0, 0x7F,
+    0xFF, 0xF0, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF};
+constexpr uint8_t icon_lightbulb_off_h_bits[] = {
+    0xFF, 0xFF, 0xFF, 0x9F, 0xFF, 0xFF, 0x8F, 0xFF, 0xFF, 0xC7, 0xFF, 0xFF, 0xE3, 0xF0, 0xFF,
+    0xF1, 0xC0, 0x7F, 0xF8, 0xFE, 0x3F, 0xF2, 0x7F, 0x3F, 0xB2, 0x3F, 0x9F, 0xB3, 0x1F, 0x9F,
+    0xB3, 0x8F, 0xDF, 0xB3, 0xC7, 0x9F, 0xF3, 0x33, 0x9F, 0xFF, 0x11, 0xFF, 0xFF, 0xC0, 0xFF,
+    0xFF, 0xF0, 0x7F, 0xFF, 0xFE, 0x3F, 0xFF, 0xFF, 0x1F, 0xFF, 0xFF, 0x9F, 0xFF, 0xFF, 0xFF};
+
 }  // namespace
 
 void BaseTheme::drawBatteryOutline(const GfxRenderer& renderer, int x, int y, int battWidth, int rectHeight,
@@ -122,6 +140,89 @@ void BaseTheme::drawBatteryRight(const GfxRenderer& renderer, Rect rect, const b
   fillBatteryIcon(renderer, iconRect, percentage, foregroundBlack);
 }
 
+void BaseTheme::drawFrontlightStatusIndicator(const GfxRenderer& renderer, int topY, bool darkMode) const {
+  // Pinned to the top-left corner, positioned entirely independently of the
+  // clock (centered) and battery (wherever its own header/status-bar logic
+  // puts it) so neither is affected.
+  //
+  // A single lamp icon, not a percentage readout: based on the exact same
+  // icon_lightbulb_28 / icon_lightbulb_off_28 the frontlight panel already
+  // draws next to its own "Brightness NN%" label (FrontlightPanelActivity),
+  // rather than hand-drawn glyphs, but rotated 90deg (head right, base left)
+  // and shrunk to 20px so it lies flat in the horizontal status bar row
+  // instead of standing upright at panel size, then flipped top-to-bottom to
+  // match how it actually renders on real hardware. A pair of 7px custom
+  // icons plus tiny percentage text was tried first and was illegible at
+  // status-bar size, and an upright 14px version of this same icon was
+  // still too small to read; 20px, laid on its side, is the size/orientation
+  // that stays legible in that thin row. The lightbulb-off variant already
+  // reads as "off" (a strike-through) on its own, so no separate number is
+  // needed to say whether the light is lit.
+  //
+  // Reads Frontlight.isOn() live on every call, the same way
+  // FrontlightPanelActivity::loop() re-syncs its own bulb icon from
+  // Frontlight.isOn() every frame (so a power-button light toggle, which
+  // can happen while any screen is open, is picked up here too).
+  if (!Frontlight.present()) {
+    return;
+  }
+
+  const auto& metrics = UITheme::getInstance().getMetrics();
+  const int statusBarHeight = std::max(UITheme::getStatusBarHeight(), metrics.statusBarVerticalMargin);
+  if (statusBarHeight <= 0) {
+    return;
+  }
+
+  int orientedMarginTop, orientedMarginRight, orientedMarginBottom, orientedMarginLeft;
+  renderer.getOrientedViewableTRBL(&orientedMarginTop, &orientedMarginRight, &orientedMarginBottom,
+                                   &orientedMarginLeft);
+  (void)orientedMarginRight;
+  (void)orientedMarginBottom;
+
+  const int baseTopY = topY >= 0 ? topY : orientedMarginTop + metrics.topPadding;
+  const int iconY = baseTopY + (statusBarHeight - frontlightStatusIconSize) / 2;
+  const int x = orientedMarginLeft + metrics.statusBarHorizontalMargin;
+
+  const uint8_t* bits = Frontlight.isOn() ? icon_lightbulb_h_bits : icon_lightbulb_off_h_bits;
+  if (darkMode) {
+    renderer.drawIconInverted(bits, x, iconY, frontlightStatusIconSize);
+  } else {
+    renderer.drawIcon(bits, x, iconY, frontlightStatusIconSize);
+  }
+}
+
+bool BaseTheme::isFrontlightIndicatorTap(const GfxRenderer& renderer, int topY, int touchX, int touchY) const {
+  if (!Frontlight.present()) {
+    return false;
+  }
+
+  const auto& metrics = UITheme::getInstance().getMetrics();
+  const int statusBarHeight = std::max(UITheme::getStatusBarHeight(), metrics.statusBarVerticalMargin);
+  if (statusBarHeight <= 0) {
+    return false;
+  }
+
+  int orientedMarginTop, orientedMarginRight, orientedMarginBottom, orientedMarginLeft;
+  renderer.getOrientedViewableTRBL(&orientedMarginTop, &orientedMarginRight, &orientedMarginBottom,
+                                   &orientedMarginLeft);
+  (void)orientedMarginRight;
+  (void)orientedMarginBottom;
+
+  const int baseTopY = topY >= 0 ? topY : orientedMarginTop + metrics.topPadding;
+
+  // Generous hit box, not the icon's own tight bounds: the whole top status
+  // bar row's height, and from the screen edge out to well past the icon's
+  // right edge, since a 20px glyph is a hard target to hit precisely with a
+  // finger.
+  constexpr int tapPaddingRight = 24;
+  const int hitLeft = 0;
+  const int hitRight = orientedMarginLeft + metrics.statusBarHorizontalMargin + frontlightStatusIconSize + tapPaddingRight;
+  const int hitTop = baseTopY;
+  const int hitBottom = baseTopY + statusBarHeight;
+
+  return touchX >= hitLeft && touchX < hitRight && touchY >= hitTop && touchY < hitBottom;
+}
+
 int BaseTheme::homeHeaderClockTextYOffset(const GfxRenderer& renderer) {
   const auto& metrics = UITheme::getInstance().getMetrics();
   const int statusBarHeight = std::max(UITheme::getStatusBarHeight(), metrics.statusBarVerticalMargin);
@@ -1045,6 +1146,12 @@ void BaseTheme::drawStatusBar(GfxRenderer& renderer, const float bookProgress, c
 void BaseTheme::drawTopStatusBarClock(const GfxRenderer& renderer, int topY, const char* previewTime,
                                       const bool readerContext, const int textYOffset, const bool darkMode,
                                       const bool forceVisible) const {
+  // Deliberately ahead of the clock's own visibility/availability checks
+  // below, so the frontlight indicators keep rendering even when the clock
+  // is hidden (no RTC, or the user turned the clock off) - this function is
+  // the one place every screen, reader and non-reader alike, already calls.
+  drawFrontlightStatusIndicator(renderer, topY, darkMode);
+
   if (!forceVisible &&
       !(readerContext ? SETTINGS.shouldShowClockInReader() : SETTINGS.shouldShowClockOutsideReader())) {
     return;
diff --git a/src/components/themes/BaseTheme.h b/src/components/themes/BaseTheme.h
index 714d1277..628a4602 100644
--- a/src/components/themes/BaseTheme.h
+++ b/src/components/themes/BaseTheme.h
@@ -243,6 +243,28 @@ class BaseTheme {
                         bool foregroundBlack = true) const;  // Right aligned (UI headers)
   virtual void fillBatteryIcon(const GfxRenderer& renderer, Rect rect, uint16_t percentage,
                                bool foregroundBlack = true) const;
+  // Frontlight on/off indicator, pinned to the top-left corner: reuses the
+  // same lightbulb / lightbulb-off icon the frontlight panel already draws
+  // next to its "Brightness" label, downscaled to status-bar size, instead
+  // of a percentage readout. Reads Frontlight.isOn() live on every call
+  // (not the settings copy, which only syncs when the frontlight panel
+  // closes), so it always reflects the current state. Deliberately
+  // independent of the clock/battery layout: called from
+  // drawTopStatusBarClock() - the one function every screen (reader and
+  // non-reader) already calls - ahead of its own visibility checks, so it
+  // renders every time regardless of clock or battery settings.
+  void drawFrontlightStatusIndicator(const GfxRenderer& renderer, int topY, bool darkMode) const;
+  // Hit-test for the indicator above: shares its geometry (same topY
+  // argument) so the tappable zone always matches where the icon is drawn,
+  // padded out generously since the icon itself is only 20px and hard to
+  // hit precisely with a finger. Callers that want the indicator tappable
+  // check this on a tap before falling through to their own tap handling
+  // (page turn, status bar hide, ...), and on a hit call
+  // Frontlight.setOn(!Frontlight.isOn()) + requestUpdate() themselves - this
+  // only answers "was the tap on the indicator", it does not toggle the
+  // light or consume the touch itself, since callers differ in what "handle
+  // this touch" means for them.
+  bool isFrontlightIndicatorTap(const GfxRenderer& renderer, int topY, int touchX, int touchY) const;
   // Button hint labels use three states: non-empty labels draw an active hint,
   // an empty string clears an inactive slot, and nullptr preserves the existing
   // background without drawing or registering a target.
